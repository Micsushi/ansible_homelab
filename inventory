[homeserver]
localhost ansible_connection=local

# Alternative: If running from remote machine via SSH, use:
# <server_ip>
# [homeserver:vars]
# ansible_user = <username>
# ansible_ssh_private_key_file = <path/to/private/key>
# OR
# ansible_ssh_pass = <password>